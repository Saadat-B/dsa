#include <stdio.h>

int main()
{

    int length;
    int breadth;
    scanf("%d", &length);
    scanf("%d", &breadth);

    printf("this is the area of the rectangle %d", length * breadth);

    return 0;
}